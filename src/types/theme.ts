export type Theme = {
    backgroundColor: string,
    fontFamily: string,
    authorColor: string,
    codeColor: string,
    codeBackgroundColor: string,
    questionColor: string,
    answerColor: string,
    jokeColor: string
}

export type ThemeStyle = "default";